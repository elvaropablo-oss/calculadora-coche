(()=>{
'use strict';
const API='https://energia.serviciosmin.gob.es/ServiciosRestCarburantes/PreciosCarburantes/EstacionesTerrestres/FiltroProducto/';
const PRODUCTS={gasolina:{id:1,name:'Gasolina 95 E5'},diesel:{id:4,name:'Gasóleo A'}};
const $=s=>document.querySelector(s);
const state={map:null,markers:null,userMarker:null};

function n(v){if(typeof v==='number')return v;return Number(String(v??'').replace(',','.').replace(/[^0-9.-]/g,''))}
function esc(v){return String(v??'').replace(/[&<>'"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[m]))}
function km(lat1,lon1,lat2,lon2){const R=6371,dLat=(lat2-lat1)*Math.PI/180,dLon=(lon2-lon1)*Math.PI/180,a=Math.sin(dLat/2)**2+Math.cos(lat1*Math.PI/180)*Math.cos(lat2*Math.PI/180)*Math.sin(dLon/2)**2;return 2*R*Math.asin(Math.sqrt(a))}
function price(v){return Number(v).toLocaleString('es-ES',{minimumFractionDigits:3,maximumFractionDigits:3})+' €/L'}
function getCoords(r){const lat=n(r.Latitud??r.latitud??r.Latitude),lon=n(r['Longitud (WGS84)']??r.Longitud??r.longitud??r.Longitude);return Number.isFinite(lat)&&Number.isFinite(lon)?{lat,lon}:null}
function setStatus(msg,type=''){const el=$('#nearStatus');if(!el)return;el.textContent=msg;el.dataset.type=type}

function getLocation(){return new Promise((resolve,reject)=>{if(!navigator.geolocation){reject(new Error('Tu navegador no admite geolocalización.'));return}navigator.geolocation.getCurrentPosition(p=>resolve({lat:p.coords.latitude,lon:p.coords.longitude,accuracy:p.coords.accuracy}),e=>{const m=e.code===1?'Has denegado el permiso de ubicación. Puedes activarlo en los permisos del navegador y volver a intentarlo.':e.code===2?'No se ha podido determinar tu ubicación.':'La ubicación ha tardado demasiado en responder.';reject(new Error(m))},{enableHighAccuracy:false,timeout:12000,maximumAge:300000})})}

async function loadLeaflet(){if(window.L)return;await new Promise((resolve,reject)=>{const css=document.createElement('link');css.rel='stylesheet';css.href='https://unpkg.com/leaflet@1.9.4/dist/leaflet.css';css.integrity='sha256-p4NxAoJBhIINfQ3ynHTeKX5QKfZbMZKxY6YZp7jQeqg=';css.crossOrigin='';document.head.appendChild(css);const s=document.createElement('script');s.src='https://unpkg.com/leaflet@1.9.4/dist/leaflet.js';s.integrity='sha256-20nQCchB9co0qIjJZRGuk2/Z9VM+kNiyxNV1lvTlZBo=';s.crossOrigin='';s.onload=resolve;s.onerror=()=>reject(new Error('No se ha podido cargar el mapa interactivo.'));document.head.appendChild(s)})}

async function loadStations(kind){const cfg=PRODUCTS[kind]||PRODUCTS.gasolina;const r=await fetch(API+cfg.id,{headers:{Accept:'application/json'}});if(!r.ok)throw new Error('El servicio oficial de precios no responde ahora mismo.');const d=await r.json();const rows=Array.isArray(d.ListaEESSPrecio)?d.ListaEESSPrecio:[];return{date:d.Fecha||'actualización reciente',rows:rows.map(x=>{const c=getCoords(x),p=n(x.PrecioProducto);if(!c||!Number.isFinite(p)||p<.5||p>3.5)return null;return{...x,...c,price:p}}).filter(Boolean)}}

function renderResults(items,kind,date){const root=$('#nearResults');const count=$('#nearCount');if(!root)return;count.textContent=`${items.length} resultado${items.length===1?'':'s'} mostrado${items.length===1?'':'s'} · ${date}`;if(!items.length){root.innerHTML='<div class="near-empty"><strong>No encontramos estaciones dentro de ese radio.</strong><span>Prueba con 20 o 50 km.</span></div>';return}const cheapest=items[0].price;root.innerHTML=items.map((s,i)=>{const brand=s['Rótulo']||s.Rotulo||'Estación de servicio',addr=[s['Dirección']||s.Direccion,s.Municipio].filter(Boolean).join(' · '),schedule=s.Horario||'';return `<article class="near-result ${i===0?'is-cheapest':''}"><div class="near-rank">${i+1}</div><div class="near-result-main"><div class="near-result-title">${esc(brand)} ${i===0?'<span>Más barata</span>':''}</div><div class="near-result-address">${esc(addr||'Dirección no disponible')}</div>${schedule?`<div class="near-result-hours">${esc(schedule)}</div>`:''}</div><div class="near-result-price"><strong>${price(s.price)}</strong><span>${s.distance.toLocaleString('es-ES',{maximumFractionDigits:1})} km</span><a href="https://www.google.com/maps/dir/?api=1&destination=${encodeURIComponent(s.lat+','+s.lon)}" target="_blank" rel="noopener">Cómo llegar ↗</a></div></article>`}).join('');const spread=items.length>1?items.at(-1).price-cheapest:0;const summary=$('#nearSummary');summary.innerHTML=`La más barata de los resultados mostrados está a <strong>${price(cheapest)}</strong>. ${spread>0?`Entre la primera y la última hay <strong>${(spread*100).toLocaleString('es-ES',{maximumFractionDigits:1})} cént./L</strong> de diferencia.`:''}`}

async function renderMap(user,items){const box=$('#nearMap');if(!box)return;try{await loadLeaflet();if(!state.map){state.map=L.map(box,{scrollWheelZoom:false}).setView([user.lat,user.lon],11);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'&copy; OpenStreetMap contributors'}).addTo(state.map);state.markers=L.layerGroup().addTo(state.map)}else{state.markers.clearLayers();if(state.userMarker)state.map.removeLayer(state.userMarker)}state.userMarker=L.circleMarker([user.lat,user.lon],{radius:8,weight:3,fillOpacity:.9}).addTo(state.map).bindPopup('Tu ubicación aproximada');const bounds=[[user.lat,user.lon]];items.slice(0,20).forEach((s,i)=>{const m=L.marker([s.lat,s.lon]).bindPopup(`<strong>${esc(s['Rótulo']||s.Rotulo||'Estación')}</strong><br>${price(s.price)} · ${s.distance.toLocaleString('es-ES',{maximumFractionDigits:1})} km`);m.addTo(state.markers);bounds.push([s.lat,s.lon])});if(bounds.length>1)state.map.fitBounds(bounds,{padding:[28,28],maxZoom:13});else state.map.setView([user.lat,user.lon],12);setTimeout(()=>state.map.invalidateSize(),60)}catch(e){box.innerHTML=`<div class="near-map-error"><strong>Mapa no disponible.</strong><span>${esc(e.message)}</span></div>`}}

async function search(){const btn=$('#nearSearch'),kind=$('#nearFuel')?.value||'gasolina',radius=Number($('#nearRadius')?.value||10);btn.disabled=true;btn.textContent='Buscando…';setStatus('Solicitando tu ubicación al navegador…');$('#nearResults').innerHTML='<div class="near-loading">Buscando precios oficiales y calculando distancias…</div>';$('#nearCount').textContent='';$('#nearSummary').textContent='';try{const user=await getLocation();setStatus('Ubicación recibida. Consultando precios oficiales…','ok');const d=await loadStations(kind);const all=d.rows.map(s=>({...s,distance:km(user.lat,user.lon,s.lat,s.lon)})).filter(s=>s.distance<=radius).sort((a,b)=>a.price-b.price||a.distance-b.distance);const shown=all.slice(0,15);renderResults(shown,kind,d.date);await renderMap(user,shown);setStatus(`Encontradas ${all.length} estaciones dentro de ${radius} km. Mostramos las 15 más baratas.`,'ok');window.CosteCocheAnalytics?.track?.('nearby_fuel_search',{fuel:kind,radius_km:radius,results_bucket:all.length===0?'0':all.length<6?'1-5':all.length<16?'6-15':'16+'})}catch(e){setStatus(e.message||'No se ha podido completar la búsqueda.','error');$('#nearResults').innerHTML='<div class="near-empty"><strong>No hemos podido completar la búsqueda.</strong><span>Revisa el permiso de ubicación o vuelve a intentarlo dentro de unos minutos.</span></div>';$('#nearCount').textContent=''}finally{btn.disabled=false;btn.textContent='Buscar gasolineras cerca de mí'}}

function init(){const btn=$('#nearSearch');if(btn)btn.addEventListener('click',search)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();